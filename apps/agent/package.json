{
  "name": "@keel/agent",
  "description": "keel agent — a Claude Code surface: focus gate + activity-log writer. Ships as a Claude Code plugin.",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "bin": {
    "keel": "./keel.mjs"
  },
  "scripts": {
    "test": "node --test",
    "typecheck": "tsc --noEmit --checkJs --allowJs --target es2022 --module nodenext --moduleResolution nodenext core.mjs store.mjs keel.mjs capture.mjs capture-store.mjs keel-classify.mjs"
  },
  "devDependencies": {
    "@types/node": "^22.15.0",
    "typescript": "~5.8.3"
  }
}
