# keel e-ink ambient surface (the calm face)

- An e-ink display as a keel surface — the *calm ambient face of keel*. Shows today's intention / tide / `keel retreat` state, glanceable, always-on, no pull.
- **Why e-ink is on-thesis, not a gadget:** Calm Interface + Peripheral Presence embodied — no backlight, static (no motion/dopamine), glanceable, low-power. The anti-phone. Especially apt as the surface you look at *during* a content retreat instead of a glowing feed.
- Plausibly the **home for the `keel retreat` primary affordance** (see [[keel-as-attention-control-plane]]) — the disconnect-state and the day's one intention live here, on a surface that can't itself become a feed.

- **Rafa already owns a TRMNL** → no purchase, no hardware build. Integration = a keel **screen-generator** (render → push via TRMNL's plugin API / self-hostable server). Pure software, small. This is the path; the Pi + Waveshare DIY route is moot.

- Questions:
  - What does the screen actually show — just today's intention, or tide + retreat-state + a read-later count?
  - Self-host TRMNL server, or accept their hosted one for v1?
  - Does this replace/complement the tray surface (apps/tray)?

Don't shape yet. NOT a retreat purchase — this is a separate keel-surface bet. Shape post-retreat with the primary-affordance brainstorm.
